# Supabase Configuration
# Copy this file to .env.local and fill in your values
# Get these values from your Supabase project settings → API

# Public variables (exposed to browser - safe with Row Level Security enabled)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side only (NEVER add NEXT_PUBLIC_ prefix to these)
# Used for admin operations in Server Actions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database connection (for migrations and direct DB access)
# Get from Supabase → Settings → Database → Connection string (pooler)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-1-us-east-1.pooler.supabase.com:6543/postgres
